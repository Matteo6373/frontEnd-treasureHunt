import { ThemeImagePipe } from './theme-image-pipe';

describe('ThemeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
